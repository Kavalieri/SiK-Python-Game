# Test YAML File para hooks
# Este archivo debe ser valido pero podria tener problemas

name: "Test Config"
version: 1.0

# Configuracion de prueba
settings:
  debug: true
  timeout: 30
  features:
    - "feature1"
    - "feature2"
    - "feature3"

# Seccion con posibles problemas de formato
database:
    host: "localhost"
    port: 5432
    name: "testdb"

# Trailing spaces al final
networks:
  default:
    external: true

# Final del archivo sin newline
